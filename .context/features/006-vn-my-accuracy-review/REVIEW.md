# Review: VN/MY Accuracy & Code-Correctness Review

> Note: the `feature-reviewer` subagent stalled on a stream-watchdog timeout mid-pass (infrastructure, not a logical failure — it had already confirmed the audit commits' scope). The evidence gate below was therefore run fresh by the dispatching session (Opus 4.8) directly, in this verify pass. All commands and outputs are from this pass, not an executor's earlier report.

## Task-level check

Nine tasks, two kinds. This feature is unusual: 6 of 9 tasks produce a **findings markdown document** as their deliverable, not code — for those, "matches spec" means the document exists with the required structure and was produced read-only (no code/config/data edits). Only 3 tasks (007/008/009) are code fixes.

- **001 refetch VN** — `refetched/vn_sources.json` present, 15 sources. Commit `dc7eec9` touched only that cache file + task md. ✓
- **002 refetch MY** — `refetched/my_sources.json` present, 25 sources. Commit `4e833d3` touched only that cache file + task md. ✓
- **003 accuracy audit VN** — `ACCURACY-AUDIT.md` `## Vietnam` section (line 8). Commit `0bff3f0` touched only `ACCURACY-AUDIT.md` + task md; no code. ✓
- **004 accuracy audit MY** — `ACCURACY-AUDIT.md` `## Malaysia` section (line 44), VN section left intact. Commit `c5532f2` touched only `ACCURACY-AUDIT.md` + task md. ✓ (Disclosed limitation: MY PDF unreadable in executor env — poppler/`pdftoppm` absent — so audited against live-refetched content + config cross-check; disclosed in the doc's Coverage note. Acceptable — refetched pages are the stronger ground truth anyway.)
- **005 code review §1** — `CODE-REVIEW.md` `## 1.` section (line 35). Commit `c5b6e27` touched only `CODE-REVIEW.md` + task md; no code. ✓
- **006 code review §2** — `CODE-REVIEW.md` `## 2.` section (line 170), §1 intact. Commit `dee6d46` touched only `CODE-REVIEW.md` + task md. ✓
- **007 app.py fix** — 8-domain fallback tuple present at `app.py:110` matching `_domain_mode()`'s `app.py:83`. Commit `14adeb3` touched only `app.py` + task md. Matches task's exact before/after. ✓
- **008 analyst.py fix** — `os.environ.get("GROQ_API_KEY", "")` at `analyst.py:341`. Commit `106ca9b` touched only `pipeline/analyst.py` + task md. ✓
- **009 weekly.py fix** — `{country_name}` placeholder (`weekly.py:11`), signature default (`weekly.py:25`), `.replace()`-before-`.format()` (`weekly.py:68`), main.py call site (`main.py:106`). Commit `0a3f907` touched only `pipeline/weekly.py` + `main.py` + task md. All 4 edits present, ordering correct. ✓

No task drifted from its spec.

## Decision coverage

Against CONTEXT.md's Implementation Decisions + Global Constraints:

- **Bundled as one feature** — done (single feature dir, 9 tasks). ✓
- **Re-fetch live source pages via existing `scrape_source()`** — done; both caches produced, scoped to exactly the cited sources (15 VN + 25 MY), no new scraping mechanism. ✓
- **Fix tasks separate + optional, each a distinct commit** — done: 007→`14adeb3`, 008→`106ca9b`, 009→`0a3f907`, three independent commits, each revertible/cherry-pickable. ✓
- **Deliverable format = structured findings markdown** (`ACCURACY-AUDIT.md`, `CODE-REVIEW.md`) — done, both present with required section headers. ✓
- **No silent "fix while reviewing"** — verified: none of the 6 audit/review commits touched any `.py`/`.json` config/data/pipeline file; findings were surfaced, not patched. The only code changes are the 3 explicitly-approved optional fixes. ✓
- **HARD: no Groq/LLM calls, no `py main.py` run** — verified from the diff: the report JSONs (`latest_report_VN_BER.json` / `MY_GENERAL.json` / `run_metadata_*`) were touched ONLY in the planning commit `e0bb371` (as audit inputs), never regenerated by any task. No pipeline execution occurred. ✓
- **Model-tier pinning (2026-07-10 directive: audits→Sonnet, code review→Opus)** — task files 003/004 pin `mid`/Sonnet, 005/006 pin `quality`/Opus; dispatched accordingly. (Verifiable from task-file Model tier lines + the execution ledger, not from the diff.) ✓

No decision quietly dropped or changed.

## Goal alignment

Goal: *audit whether the three features shipped this session produce accurate, non-hallucinated report content and correctly-wired code, surfacing findings + optional fixes without a live pipeline run.*

Satisfied. The feature delivered both halves — an accuracy audit finding the reports are mostly grounded (VN 38/43, MY 8/9 signals) with the opportunity `product_fit` field as the consistent weak spot (fabricated product names), and a code-correctness review whose standout result is the `source_name` breakage, git-dated to before this session (a testing-coverage gap that passed three green `/feature-verify` gates, not a rapid-dispatch regression). Three low-risk mechanical fixes were applied on user approval; the deeper findings (source_name root cause, gate divergence, dead RAG) were flagged for judgment rather than force-fixed. No Groq quota was touched.

## Evidence

All run fresh in this verify pass (dispatching session, Opus 4.8):

1. **Audit/review/refetch commit scope** — `git show --stat` on `dc7eec9 4e833d3 c5b6e27 0bff3f0 dee6d46 c5532f2`: each touched only its markdown/cache deliverable + its own task file. No code/config/data.
2. **Fix commit scope** — `git show --stat` on `106ca9b 14adeb3 0a3f907`: `analyst.py` only / `app.py` only / `weekly.py`+`main.py` only, each + task md.
3. **AST parse** — `py -c "import ast; [ast.parse(open(f,encoding='utf-8').read()) for f in ('app.py','pipeline/analyst.py','pipeline/weekly.py','main.py')]; print('all parse OK')"` → `all parse OK` (exit 0).
4. **Fix presence** —
   - `app.py:110` → `for d in ("EDU", "BER", "GENERAL", "RCC", "HLS", "MFG", "CTE", "PSS")  # keep in sync with _domain_mode()`
   - `analyst.py:341` → `client = Groq(api_key=os.environ.get("GROQ_API_KEY", ""))`
   - `weekly.py:11/25/68` → `{country_name}` placeholder + `country_name: str = "Singapore"` + `WEEKLY_PROMPT.replace("{country_name}", country_name)`
   - `main.py:106` → `generate_weekly_summary(country_code=country["code"], country_name=country["name"])`
5. **Deliverable structure** — `ACCURACY-AUDIT.md` has `## Vietnam` (8) + `## Malaysia` (44); `CODE-REVIEW.md` has `## 1.` (35) + `## 2.` (170).
6. **No-LLM-run guard** — `git log 16db787..HEAD -- data/latest_report_VN_BER.json data/latest_report_MY_GENERAL.json data/run_metadata_VN.json data/run_metadata_MY.json` → only `e0bb371` (planning commit). No task regenerated a report.
7. **Refetch caches** — VN 15 sources, MY 25 sources loaded cleanly.

Deliberately NOT run: `py main.py` or any LLM call site — running one would itself violate the feature's hard constraint. The evidence gate here is AST-parse + presence + structure + scope checks by design, not a pipeline run.

## Result

PASS — fix tasks generated: none.
